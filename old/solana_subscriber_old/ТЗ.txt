📘 Техническое задание: микросервис solana_subscriber (v1.5)
🎯 Назначение
solana_subscriber — микросервис, который:

Подключается ко всем WebSocket и HTTP RPC Solana

Получает команды подписки через Redis Stream

Подписывается на onLogs и получает транзакции по getParsedTransaction

Отправляет успешные транзакции в Redis Stream (regular или spl_token)

Гарантирует "at-least-once" доставку, порядок (по возможности), и повторную обработку

Реализует защиту от отказов RPC и Redis, включая восстановление логов

Поддерживает hot-reload конфигурации из PostgreSQL

Выполняет graceful shutdown при завершении

🗃️ PostgreSQL
Таблица: subscriptions
поле	тип	описание
chain_id	TEXT	Идентификатор цепочки
account	TEXT	Pubkey аккаунта
subscription_type	TEXT	"regular" или "spl_token"
active	BOOLEAN	Активна ли подписка
last_signature	TEXT	Последняя подтверждённая успешная сигнатура
created_at	TIMESTAMP	Дата добавления
updated_at	TIMESTAMP	Дата последнего обновления
Таблица: subscriber_config
поле	тип	описание
id	SERIAL	PK
rpc_endpoints	JSONB	{ http, ws, rate_limits }[]
control_accounts	TEXT[]	Массив Pubkey контрольных аккаунтов
silence_threshold_ms	INTEGER	Порог "тишины" в миллисекундах
queue_max_length	INTEGER	Зарезервировано для ограничения FIFO
rpc_timeout_ms	INTEGER	Таймаут всех RPC-запросов (например, 5000 мс)
updated_at	TIMESTAMP	Дата последнего обновления
🔄 Команды из Redis Stream subscriber_control
json

{
  "action": "subscribe",
  "chain_id": "chain1",
  "account": "Pubkey",
  "subscription_type": "regular" | "spl_token",
  "last_signature": "опционально"
}
json

{
  "action": "unsubscribe",
  "chain_id": "chain1",
  "account": "Pubkey"
}
json

{
  "action": "update_config"
}
🚀 Инициализация микросервиса
При запуске:

Загружается конфигурация из БД (subscriber_config)

Подключаются все WebSocket и HTTP RPC

Создаются Token Bucket лимитаторы по RPC

Загружаются активные подписки из БД (subscriptions)

Производится подписка на аккаунты (onLogs)

Запускается восстановление логов по last_signature (или полное, если не указано)

Стартует Redis consumer и очереди (onLogsQueue, retryQueue)

🧼 Graceful Shutdown
При получении сигнала завершения (SIGINT, SIGTERM):

Отключается Redis

Закрываются RPC соединения

Все активные onLogs подписки удаляются

Останавливаются очереди

Логируется и завершается процесс

📡 Обработка onLogs
При получении события — проверяется лимит

Вызывается getParsedTransaction с таймаутом из rpc_timeout_ms

Если транзакция:

Успешна → отправляется в Redis, last_signature обновляется

С ошибкой (err) → игнорируется, логируется

null или ошибка → добавляется в очередь повторов

♻️ Восстановление
Получает сигнатуры между last_signature и новыми (onLogsQueue)

Сортирует по slot

Вызывает getParsedTransaction (с таймаутом)

Обрабатывает только успешные

После восстановления обрабатывает кешированные onLogs

🔁 Повторная обработка null
До 3 попыток (retryQueue)

Интервал между попытками увеличивается

После maxRetry → логируется как unresolved_transaction

🧃 FIFO очередь
Сохраняет сигнатуры, отложенные из-за лимитов

При восстановлении — исключает дубли

Логирует:

json

{ "event": "queue_overflow", "type": "rate_limit" }
{ "event": "queue_resumed", "queue_length": 12 }
🔧 Обработка update_config
Загружается новая конфигурация из БД

Применяется без перезапуска:

RPC (переключение и переподключение)

контрольные аккаунты

порог тишины

таймаут RPC

лимиты

✅ Безопасная доставка
last_signature обновляется только после успешной отправки в Redis

Если Redis недоступен:

сообщение в redisRetryQueue

повтор отправки до успеха

Только после этого — обновление last_signature

🔁 Повторное subscribe
Если last_signature передан — он перезаписывает старый

Если не передан — используется текущий из БД

Логируется resubscribe

🧼 Переключение WebSocket RPC
При disconnect:

все активные подписки удаляются

создаётся новое подключение

подписки пересоздаются

🚦 Контрольные аккаунты
Всегда подписаны

Если неактивны дольше silence_threshold_ms:

запрашиваем getSignaturesForAddress

если есть новые → активируем восстановление для всех

📤 Отправка в Redis Stream
Stream'ы:
solana_logs_regular

solana_logs_spl

Формат сообщения:
json
Копировать
Редактировать
{
  "chain_id": "chain1",
  "account": "Pubkey",
  "signature": "abc123",
  "log": { ...parsedTransaction },
  "subscription_type": "regular" | "spl_token",
  "blockTime": 1700000000,
  "timestamp": 1710000000000
}
📊 Логирование (для Grafana)
Поле	Назначение
chain_id	Идентификатор цепочки
account	Адрес аккаунта
signature	Сигнатура транзакции
rpc_id	Идентификатор RPC узла
type	Событие: subscribe, unsubscribe, onLogs, recovery, resubscribe, rate_limit, config_update, ws_disconnect, reconnect, failed_transaction, unresolved_transaction
subscription_type	Тип подписки: "regular" или "spl_token"
📦 На будущее
queue_max_length → лимит на FIFO

Метрики Prometheus

/metrics endpoint

OpenTelemetry или трейсинг

Поддержка приоритетных аккаунтов

===================================

✅ Вот правильная структура solana_subscriber/ с учётом sharedLogger.js:

/project-root/
├── utils/
│   ├── sharedLogger.js              ✅ Централизованный логгер (используется во всех микросервисах)
│   └── redisClient.js               ✅ Клиент Redis, обёртка над ioredis
│
├── services/
│   └── solana_subscriber/
│       ├── Dockerfile
│       ├── package.json
│       ├── .env
│       ├── index.js                 ✅ Точка входа, управление жизненным циклом
│
│       ├── config/
│       │   ├── configLoader.js      ✅ Загрузка и hot-reload конфигурации из PostgreSQL
│       │   └── redisConsumer.js     ✅ Получение команд подписки/отписки из Redis
│
│       ├── db/
│       │   ├── db.js                ✅ Подключение к PostgreSQL (pg или pg-promise)
│       │   ├── subscriptions.js     ✅ CRUD подписок (таблица `subscriptions`)
│       │   └── subscriberConfig.js  ✅ CRUD по `subscriber_config`
│
│       ├── rpc/
│       │   ├── rpcPool.js           ✅ Подключение и управление RPC (ws + http)
│       │   ├── rpcLimiter.js        ✅ Token Bucket для RPC лимитов
│       │   └── rpcUtils.js          ✅ Таймауты, переключение RPC, утилиты
│
│       ├── subscription/
│       │   ├── subscriptionManager.js ✅ Управление onLogs подписками
│       │   ├── recoveryManager.js     ✅ Восстановление транзакций при сбое
│       │   ├── onLogsHandler.js       ✅ Обработка логов и транзакций
│       │   └── inflightTracker.js     ✅ Защита от “скачков” сигнатур
│
│       ├── queue/
│       │   ├── onLogsQueue.js         ✅ FIFO очередь при превышении лимита
│       │   └── retryQueue.js          ✅ Повторы getParsedTransaction при null/ошибках
│
│       └── utils/
│           ├── time.js                ✅ Работа с blockTime, timestamp, интервалы
│           └── errorHandler.js        ✅ tryWrapAsync(), глобальные try-catch


